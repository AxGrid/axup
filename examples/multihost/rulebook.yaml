name: multihost-demo

vars:
  env: unknown   # default; overridden per-host via inventory.yaml
  node: unknown
  target: /opt/multihost-default.txt

bootstrap:
  - name: write per-host note
    template:
      src: templates/note.tmpl
      dst: "{{ .target }}"
      mode: "0644"

  - name: announce
    command: 'echo "{{ .env }}/{{ .node }} wrote {{ .target }}"'
    when_changed:
      - "{{ .target }}"
