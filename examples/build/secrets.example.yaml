# Template for examples/build/secrets/registry.axgrid.com.yaml
#
# Copy this file:
#   mkdir -p examples/build/secrets
#   cp examples/build/secrets.example.yaml examples/build/secrets/registry.axgrid.com.yaml
#
# Then edit and put real creds in. The secrets/ directory is gitignored.
# A future release will support age/sops-encrypted variants of this file
# transparently — the path stays the same.

username: your-registry-username
password: your-registry-password
